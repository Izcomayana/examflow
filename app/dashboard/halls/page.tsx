import { HallsContent } from "@/components/(dashboard)/(content)/halls"

export const metadata = {
  title: 'Halls & Venues - ExamFlow Dashboard',
  description: 'Manage examination halls',
}

export default function HallsPage() {
  return <HallsContent />
}