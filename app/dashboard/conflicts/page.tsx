import { ConflictsContent } from "@/components/(dashboard)/(content)/conflicts"

export const metadata = {
  title: 'Conflict Center - ExamFlow Dashboard',
  description: 'Manage scheduling conflicts',
}

export default function ConflictsPage() {
  return <ConflictsContent />
}