import { GeneratorContent } from "@/components/(dashboard)/(content)/generator"

export const metadata = {
  title: 'Schedule Generator - ExamFlow Dashboard',
  description: 'Generate smart exam schedules',
}

export default function GeneratorPage() {
  return <GeneratorContent />
}
